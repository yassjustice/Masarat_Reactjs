import React from 'react';
import { Outlet } from 'react-router-dom';
import './resultLayoutStyle.css';

const ResultLayout = () => {
    return (
        <Outlet></Outlet>
    );
}

export default ResultLayout;
